out_dir: out_dir/test_plot_training_schedule


# Interpolant config
name: sd3_rf

training_t_schedule: "proteina"  # "logit_normal", "mode", "proteina"

training_t_schedule_cfg: null  # filled in based on array

training_t_schedule_cfg_array:
  logit_normal:
    # mean and std of the normal distribution
    location: [0]
    scale: [1]
  mode:
    s: [0]  # between -1 and 2 / (pi - 2)
  proteina:
    alpha: [1.9, 1.6, 1.2, 1.0]
    beta: [1.0, 1.0, 1.0, 1.0]

hydra:
  run:
    dir: /data/research/hydra_outputs # /media/scratch/huang_lab/allatom_design/hydra_outputs
