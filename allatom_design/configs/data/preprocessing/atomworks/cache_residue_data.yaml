# Basic settings
seed: 6
out_dir: /scratch/users/zhkim216/datasets/atomworks/cached_residue_data
ccd_dir: /scratch/users/zhkim216/datasets/ccd_mirror

# Parallel processing
num_workers: 8
max_file_size: null

# Sharding
shard_id: 0
num_shards: 16

### Conformer generation ###
n_trials: 3
timeout_offset: 3.0
timeout_slope: 1.0
num_conformers: 50

generate_conformers_kwargs:
  optimize: true
  num_threads: 2
  hydrogen_policy: "remove"

