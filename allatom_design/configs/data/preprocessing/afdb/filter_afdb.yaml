    # parser.add_argument("--csv_file", required=True, type=str, help="Path to the dataframe CSV with plddt_256 and plddt_512 columns.")
    # parser.add_argument("--plddt_column", choices=["avg_ca_plddt", "plddt_256", "plddt_512"], required=True, type=str, help="Column to filter on.")
    # parser.add_argument("--plddt_threshold", required=True, type=float, help="Threshold for the pLDDT column.")
    # parser.add_argument("--min_seq_len", required=True, type=int, help="Filter out any sequences with length less than this threshold")
    # parser.add_argument("--max_seq_len", required=True, type=int, help="Filter out any sequences with length greater than this threshold.")
    # parser.add_argument("--out_dir", required=True, type=str, help="Directory to save the filtered dataset.")

csv_file: null  # Path to the afdb dataframe CSV
plddt_column: null  # Column to filter on, one of ["avg_ca_plddt", "plddt_256", "plddt_512"]
plddt_threshold: null  # Threshold for the pLDDT column
rog_threshold: null  # Threshold for radius of gyration
min_seq_len: null  # Filter out any sequences with length less than this threshold
max_seq_len: null  # Filter out any sequences with length greater than this threshold
out_dir: null  # Directory to save the filtered dataset